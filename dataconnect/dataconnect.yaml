specVersion: "v1"
serviceId: "webapp-landing"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "webapp-landing-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
